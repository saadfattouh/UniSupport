<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unisupport">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".student.StudentsActivity"></activity>
        <activity android:name=".employee.AddPageActivity" />
        <activity android:name=".student.PublicChatActivity" />
        <activity android:name=".psychologist.AddReplayPsychologistActivity" />
        <activity android:name=".employee.AddReplyActivity" />
        <activity android:name=".student.PsychologistReplyViewActivity" />
        <activity android:name=".student.ChatActivity" />
        <activity android:name=".employee.MainActivityEmployee" />
        <activity android:name=".psychologist.MainActivityPsychologist" />
        <activity android:name=".VerifyAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student.MainActivityStudent" />
    </application>

</manifest>